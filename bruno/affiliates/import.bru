meta {
  name: Import Affiliates
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/affiliates/import
  body: multipartForm
  auth: none
}

body:multipart-form {
  file: @file(../../data/affiliates.csv)
}
